{
  // Install prompt in VS Code / Cursor. Codespaces installs these anyway
  // via .devcontainer/devcontainer.json; the sets must match exactly
  // (tests/test_codespaces.py enforces it).
  "recommendations": [
    "marimo-team.vscode-marimo",
    "ms-python.python",
    "ms-vscode.live-server",
    "astral-sh.ty"
  ]
}
