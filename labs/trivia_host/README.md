# Your workdir

Type the trivia host here. Protocols: `labs/s01_loop.md` … `labs/s12_judge.md`.

Keep `labs/schemas.py` and `labs/house_rules.py` unchanged if you want
`--replay` against course cassettes. `labs/reference/` is the spotter —
open only if stuck.
